CREATE TABLE champion(
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    imageIcon TEXT NOT NULL,
    title TEXT NOT NULL,
    description TEXT NOT NULL
);

getAll:
SELECT * FROM champion;

getAllOrderByName:
SELECT * FROM champion ORDER BY name ASC;

insert:
INSERT INTO champion VALUES ?;

exists:
SELECT EXISTS (SELECT 1 FROM champion);