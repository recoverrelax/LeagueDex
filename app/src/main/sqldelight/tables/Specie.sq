CREATE TABLE IF NOT EXISTS specie(
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    language TEXT
);

getAll:
SELECT * FROM specie;

insert:
INSERT OR REPLACE INTO specie VALUES ?;

exists:
SELECT EXISTS (SELECT 1 FROM specie);