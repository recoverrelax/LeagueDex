CREATE TABLE IF NOT EXISTS vehicleEntity(
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL
);

getAllByID:
SELECT * FROM vehicleEntity WHERE id = :id;

insert:
INSERT OR REPLACE INTO vehicleEntity VALUES ?;